# Python 3.12 / Apple Silicon 기준
torch>=2.6.0,<2.8.0
transformers>=4.51.0,<4.53.0
datasets>=3.0.0,<4.0.0
accelerate>=1.7.0
sentencepiece>=0.2.0
protobuf>=4.0
safetensors>=0.4
scikit-learn>=1.4
numpy>=1.26,<3.0
pandas>=2.2
onnx>=1.16
onnxruntime>=1.18
rich>=13.0
tqdm>=4.66
matplotlib>=3.8
pytest>=8.0

# 선택 사항 — LLM-as-a-judge 기준선(01-beginner/04/run_llm_judge.py)에만 필요하다.
# 이 스크립트는 텍스트를 외부 API로 전송하므로 실행은 명시적 선택이어야 한다.
# 쓰지 않을 거면 설치하지 않아도 나머지 커리큘럼은 전부 돈다.
anthropic>=0.69

