# 용어집

ML 경험이 없어도 읽을 수 있도록 풀어 썼다. 각 항목 끝의 `→` 는 그 용어를 자세히 다루는 레슨이다.

---

## 위협과 공격

- **Prompt Injection**: 사용자 입력 또는 외부 문서가 애플리케이션의 원래 지시를 바꾸려는 공격.
  → `01-beginner/01`
- **Direct Injection**: 사용자가 모델 입력에 직접 넣는 인젝션.
- **Indirect Injection**: 검색 문서, 이메일, 웹페이지, 도구 출력처럼 신뢰하지 않는 컨텍스트에 섞인 인젝션.
  사용자는 공격하지 않았는데 공격이 성립한다. → `03-advanced/01`
- **Prompt Leaking**: 시스템 프롬프트나 개발자 메시지를 추출하려는 시도. 인젝션의 한 갈래로 보기도 하고
  별도 라벨로 나누기도 한다.
- **Jailbreak**: 모델의 안전 정책이나 제한을 해제·우회하도록 유도하는 입력.
- **지시 계층 (instruction hierarchy)**: 시스템 > 개발자 > 사용자 > 외부 문서 순의 우선순위. 아래 계층이
  위 계층을 덮어쓰지 못해야 한다.
- **공격 표면 (attack surface)**: 공격 문장이 들어올 수 있는 모든 경로. 사용자 입력, 검색 결과, 업로드
  문서, 도구 출력을 포함한다.
- **dual-use**: 같은 소재가 공격이 될 수도 정상 요청이 될 수도 있는 성질. 판단 기준은 "위반·회피가
  **분석 대상**인가 **답변 목표**인가".
- **ASR (Attack Success Rate)**: 실제 대상 시스템에서 공격 목표가 성공한 비율. 탐지 recall과 **다른 지표**
  이며 이 과정은 공격 실행을 자동화하지 않는다.
- **의미 보존 변형 (semantic-preserving perturbation)**: 사람이 읽는 의미는 그대로인데 문자열은 달라지는
  변형. 표면만 바꿔 탐지기를 우회한다. → `03-advanced/02`
- **제로폭 문자 (zero-width character)**: `U+200B` 같은 폭 0의 문자. 글자 사이에 끼워 넣으면 화면에는
  아무 변화가 없지만 토큰이 전부 깨진다. **NFKC 정규화로 제거되지 않는다.** 로그를 눈으로 읽어도
  정상 문장으로 보이므로 바이트를 봐야 발견된다.
- **동형이의 문자 (homoglyph)**: 생김새가 같고 코드포인트가 다른 문자. 라틴 `a`(U+0061)와 키릴
  `а`(U+0430)가 대표적이다. 역시 NFKC로 막히지 않는다. 방어는 **스크립트 혼용 탐지**(한 단어에
  라틴과 키릴이 섞이면 의심)다.
- **유니코드 정규화 (NFKC/NFC/NFD)**: 표기가 다른 같은 문자를 하나로 모으는 표준 변환. 전각→반각,
  한글 자모 결합 등을 처리한다. **필요하지만 충분하지 않다** — 제로폭·동형이의·인코딩 포장은 통과한다.

---

## 데이터

- **라벨 (label)**: 각 샘플에 붙이는 정답. 이 커리큘럼은 `BENIGN` / `PROMPT_INJECTION` / `JAILBREAK` 셋.
- **어노테이션 (annotation)**: 사람이 라벨을 붙이는 작업. **어노테이션 가이드**는 그 기준 문서.
- **어노테이터 간 일치도 (inter-annotator agreement)**: 같은 샘플을 여러 사람이 라벨링했을 때의 일치 정도.
  낮으면 모델 문제가 아니라 정의 문제라는 신호다.
- **Hard Negative**: 공격과 비슷한 단어를 포함하지만 실제로는 정상인 문장. 오탐을 줄이는 데 결정적이다.
  → `common/data/bench/negatives.jsonl`
- **Data Leakage (누수)**: test에 있어야 할 정보가 train으로 새어 들어간 상태. 점수를 실제보다 높게 만든다.
  → `02-intermediate/01`
- **Group Split**: 같은 `group_id`를 반드시 한 split에만 배치하는 분할법. 템플릿 누수를 막는다.
- **층화 (stratification)**: 특정 속성(라벨·언어·출처)의 비율을 각 split에서 유지하도록 나누는 것.
- **near-duplicate (유사 중복)**: 문자열은 다르지만 의미가 거의 같은 문장(조사 차이, 번역, 패러프레이즈).
  해시 기반 exact 중복 검사로는 못 잡는다.
- **train / dev / test**: 학습용 / 결정용 / 최종 보고용 분할. **dev에서 고르고 test는 한 번만 연다.**
- **합성 데이터 (synthetic data)**: 템플릿이나 규칙으로 자동 생성한 데이터. 파이프라인 검증에는 좋지만
  현실 분포를 대신하지 못한다.
- **manifest**: 데이터를 재현하는 데 필요한 정보(생성기, seed, 파라미터, 한계)를 담은 파일.

---

## 모델과 학습

- **사전학습 (pre-training)**: 대량의 일반 텍스트로 언어 자체를 학습한 단계. 우리가 하지 않는다.
- **파인튜닝 (fine-tuning)**: 사전학습 모델 위에 우리 과제 데이터로 미세 조정하는 것. → `02-intermediate/03`
- **제로샷 (zero-shot)**: 그 작업의 학습 데이터를 하나도 쓰지 않고 바로 추론. → `01-beginner/04`
- **인코더 (encoder)**: 문장을 고정 길이 벡터로 바꾸는 부분.
- **헤드 (head)**: 그 벡터를 원하는 출력 형태(라벨 점수)로 바꾸는 마지막 층.
- **토크나이저 (tokenizer)**: 문자열을 모델이 받는 정수 배열로 바꾸는 도구. `cased` 모델은 대소문자를
  구분하고, `uncased`는 구분하지 않는다.
- **truncation**: 입력이 `max_len`을 넘으면 **뒤를 잘라 버리는 것**. 긴 문서의 공격을 놓치는 원인.
- **sliding window**: 긴 입력을 겹치는 조각으로 나눠 각각 판정하고 합치는 방법. recall은 오르지만 window
  수만큼 오탐 기회도 늘어난다. → `03-advanced/01`
- **stride**: sliding window에서 조각끼리 겹치는 토큰 수. 0이면 겹치지 않아 경계에 걸친 문장이 쪼개진다.
- **logits**: 모델의 원시 출력. 범위 제한이 없는 실수.
- **softmax**: logits를 합이 1인 확률로 바꾸는 함수.
- **epoch**: 전체 학습 데이터를 한 바퀴 도는 것.
- **batch**: 한 번에 처리하는 샘플 수.
- **learning rate**: 한 번에 가중치를 얼마나 움직일지의 크기.
- **과적합 (overfitting)**: 학습 데이터는 더 잘 맞히는데 처음 보는 데이터에서는 나빠지는 상태.
  `loss`는 내려가는데 `eval_loss`가 올라가면 의심한다.
- **seed 편차 / 학습 실행 간 편차**: 아무것도 안 바꾸고 다시 학습했을 때 생기는 성능 차이.
  **개선을 주장하려면 이 폭보다 큰 변화**여야 한다. 자세한 정의는 아래 '평가' 절 참고.
  → `02-intermediate/03` 과제 2
- **classifier-as-LLM**: 생성형 LLM에 토큰 1개만 생성시켜 분류기로 쓰는 방식. 라벨 토큰이 아닌 출력이
  나올 수 있어 별도 처리가 필요하다.

---

## 평가

- **accuracy**: 맞힌 비율. 클래스가 불균형하면 쉽게 속는다.
- **TP / FP / FN**: 한 라벨을 양성으로 놓았을 때 맞힘 / 헛짚음 / 놓침.
- **precision (정밀도)**: `TP/(TP+FP)`. "그렇다고 한 것 중 진짜 비율". 낮으면 헛짚는다.
- **recall (재현율)**: `TP/(TP+FN)`. "진짜 중 잡아낸 비율". 낮으면 놓친다.
- **F1**: precision과 recall의 조화평균. 한쪽이 0이면 F1도 0이다.
- **Macro F1**: 라벨별 F1을 **동일한 비중**으로 평균한 값. 소수 클래스를 무시하는 모델을 걸러낸다.
- **micro 평균**: 전체를 한 덩어리로 계산. 샘플 많은 라벨에 좌우된다.
- **Attack Recall**: 공격 샘플 중 공격(둘 중 어느 유형이든)으로 탐지된 비율. 유형을 틀려도 성공으로 센다.
- **Benign FPR**: 정상 샘플 중 공격으로 잘못 탐지된 비율. 정상 사용자 차단률.
- **confusion matrix**: "정답이 A인데 B로 예측한 건수"를 전부 적은 표. 행이 정답, 열이 예측.
- **Slice**: 언어, 입력 출처, 길이, 변형 등 특정 조건으로 나눈 평가 부분집합. **전체 점수는 다수 slice의
  점수**이므로 소수 slice를 따로 본다.
- **일반화 격차 (generalization gap)**: 학습 분포 안의 점수와 독립 데이터 점수의 차이.
- **신뢰구간 (confidence interval)**: 표본에서 잰 값이 실제 값과 얼마나 다를 수 있는지의 범위.
  표본이 작으면 매우 넓다(n=8, p=0.25 → 대략 [0.00, 0.55]).
- **부트스트랩 (bootstrap)**: 표본을 복원추출로 여러 번 다시 뽑아 지표의 분포를 만들고, 그 백분위수로
  신뢰구간을 얻는 방법. 수식 없이 어떤 지표에도 쓸 수 있다. 한계: 원본 표본이 모집단을 대표한다고
  가정하므로, 합성 데이터에서 나온 구간은 "이 합성 분포 안에서의 불확실성"이지 실제 트래픽 성능이 아니다.
  → `common/guardlab/stats.py`
- **seed 편차**: 같은 데이터·같은 설정에서 난수 seed만 바꿔 학습했을 때 지표가 흔들리는 폭.
  주의: **seed를 고정해도 재현되지 않는다.** GPU/MPS 커널은 연산 순서가 실행마다 달라질 수 있고
  부동소수점 덧셈은 결합법칙이 성립하지 않는다. 이 커리큘럼 실측에서는 같은 seed의 편차(0.131)가
  다른 seed의 편차(0.108~0.157)와 비슷했다. 정확히는 **학습 실행 간 편차**다.
  **어떤 변경의 개선폭이 이 값보다 작으면 개선이라고 부를 수 없다.** 신뢰구간과는 다른 불확실성이다
  (신뢰구간은 평가 표본이 작아서, seed 편차는 학습이 불안정해서 생긴다). → `02-intermediate/03`
- **Threshold (임계값)**: 공격 점수가 이 값 이상일 때 차단/검토로 보내는 경계. **dev에서 정하고 test에
  한 번만 적용한다.** → `02-intermediate/05`
- **Calibration (확률 보정)**: 모델 점수가 실제 정답 확률과 일치하도록 맞추는 것. 파인튜닝한 분류기의
  softmax 값은 보통 **과신**한다 — 0.9라고 말한 것들 중 실제 공격은 0.7뿐인 식이다. 그러면 threshold
  0.8은 "80% 확률"이 아니라 그냥 "이 모델에서 그 위치"이고, **재학습하면 같은 0.8이 다른 지점을
  가리킨다.** → `02-intermediate/05-error-analysis/calibration.py`
- **Reliability diagram**: 예측 확률을 구간으로 나눠 "모델이 말한 확률"과 "실제 정답 비율"을 비교한 표/그림.
  보정이 완벽하면 둘이 같다.
- **ECE (Expected Calibration Error)**: reliability diagram의 구간별 차이를 샘플 수로 가중평균한 값.
  0에 가까울수록 확률이 믿을 만하다. **보정을 고쳐도 macro F1은 변하지 않는다** — 보정은 순위를 바꾸지
  않고 확률의 눈금만 바꾸기 때문이다.

---

## 서빙과 운영

- **ONNX**: 모델을 프레임워크 중립 형식으로 저장하는 표준. 전용 런타임이 최적화해 실행한다.
- **워밍업 (warm-up)**: 측정 전에 몇 번 미리 실행하는 것. 첫 호출은 메모리 할당·캐시 미스로 유난히 느리다.
- **p50 / p95**: 지연시간의 중앙값 / 95번째 백분위수. 평균은 이상치에 흔들리므로 백분위수를 쓴다.
  **p95가 실제 사용자 불만의 원인**이다.
- **양자화 (quantization)**: 가중치를 낮은 정밀도(fp16, int8)로 줄여 메모리·속도를 개선하는 것.
  **판정이 뒤집힐 수 있으므로** 변환 후 동등성 검증이 필요하다.
- **fail-closed / fail-open**: 판정 불가 상황에서 차단하는가 통과시키는가. 보안 시스템의 기본은
  **fail-closed**("확신이 없으면 차단").
- **Circuit Breaker**: 원격 백엔드가 연속 실패하면 일정 시간 호출을 중단하는 패턴. 죽은 백엔드에
  요청을 계속 던져 전체 지연시간이 무너지는 것을 막는다.
- **품질 게이트 (quality gate)**: 지표가 기준 미달이면 배포를 막는 자동 검사. **"회귀 없음"만 보증하며
  안전을 보증하지 않는다.**
- **allow / review / block**: 3단계 정책. `review`는 애매한 사례를 사람 검토나 제한된 실행 경로로 보낸다.
  **review 비율은 운영 비용**이므로 임계값 선택에 포함해야 한다.
- **드리프트 (drift)**: ① 문서와 코드가 어긋나는 것 ② 실제 입력 분포가 학습 시점과 달라지는 것.
- **Defense in Depth (방어 심층화)**: 탐지 모델, 권한 통제, 도구 allowlist, 컨텍스트 경계, 출력 검증 등
  여러 방어층을 겹치는 설계. **탐지기가 뚫려도 권한이 보호되어야 한다.**
- **모델 카드 (model card)**: 이 모델이 무엇이고 어디에 쓰면 안 되는지 적은 문서. intended use,
  out-of-scope use, training data, metrics, limitations를 포함한다.
